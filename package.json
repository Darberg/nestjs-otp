{
  "name": "@nestjs/otp",
  "version": "1.0.0",
  "description": "A secure and easy-to-use OTP generation and validation service for NestJS applications, supporting both TOTP and HOTP methods for user authentication.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "darberg",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "buffer": "^6.0.3",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
